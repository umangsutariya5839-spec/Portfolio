# Leave MONGODB_URI empty to run the site straight from data/portfolio.json.
# Set it to turn on the database: content and contact messages are then stored in MongoDB.
MONGODB_URI=
MONGODB_DB=umang_portfolio

# Password for /admin. Change this before you deploy.
ADMIN_PASSWORD=change-me-now
